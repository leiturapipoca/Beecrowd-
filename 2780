#include <stdio.h>
 
int main() {
 
   int d;
   scanf("%d", &d);
   
   if(d>=1401 && d<=2000){
       printf("3\n");
   }
   else if(d>=801 && d<= 1400){
       printf("2\n");
   }
   else if(d<=800 && d>=0){
       printf("1\n");
   }
    return 0;
}
